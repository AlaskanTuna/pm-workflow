---
description: Refresh an existing pm-workflow scaffold (templates, agents, model profile, Codex mode) against the installed skill.
---

Invoke the **pm-workflow** skill and run **Phase A′ — Upgrade an existing scaffold** exactly as the skill defines it in this project.

Run this only on an already-scaffolded project. If `docs/roles.md` is absent, point the human at `/pm-workflow:setup` instead.
